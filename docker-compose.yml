version: '3.9'
services:
  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgres://admin:lehia2006@chuyannam-clearwaydb-cis120:5432/clearway
      PORT: 3001
    restart: always

  frontend:
    build: ./frontend/clearway-ui
    depends_on:
      - backend
    restart: always

volumes:
  pgdata: